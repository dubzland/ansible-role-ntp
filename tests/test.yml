---
- hosts: localhost
  remote_user: root
  roles:
    - dubzland-ntp
  tasks:
    - name: grap ntp.conf
      command: /bin/cat /etc/ntp.conf
      register: ntp_conf
    - name: dump ntp.conf
      debug:
        msg: "{{ ntp_conf.stdout_lines }}"
      when:
        ntp_conf is defined
