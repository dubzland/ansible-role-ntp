---
- hosts: localhost
  remote_user: root
  roles:
    - role: dubzland-ntp
      vars:
        ntp_server_pool:
          - 10.0.10.1/24
  tasks:
    - name: grab ntp.conf
      command: /bin/cat /etc/ntp.conf
      register: ntp_conf
    - name: dump ntp.conf
      debug:
        msg: "{{ ntp_conf.stdout_lines }}"
      when:
        ntp_conf is defined
